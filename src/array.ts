const names = ['abul', 'kabul'];
const rollNumber: number[] = [12, 55, 22];

rollNumber.map(roll => roll)
names.map(name => name)

const myName = ['baser', 'kabul', 555, true];

const user: [number, string] = [55, 'ashraf'];